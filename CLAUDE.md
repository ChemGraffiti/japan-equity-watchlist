# CLAUDE.md

## プロジェクト概要 / Project Overview

このプロジェクト「日本銘柄分類」は、ユーザーが保有・監視する日本株ウォッチリスト（[japan_equity_sector_map_v2.html](japan_equity_sector_map_v2.html)、113銘柄・11大分類・41小分類）について、
①各銘柄のファンダメンタルデータ（株価・PER・PBR・信用倍率・次回決算日・IR等）を継続的に記録し、
②その記録と当日のニュース・世界情勢を踏まえて「今日の相場がどう動くか」を主観・客観に分けてコメントできる状態を維持することを目的とする。

This project, "Japan Equity Classification," maintains the user's Japanese stock watchlist ([japan_equity_sector_map_v2.html](japan_equity_sector_map_v2.html), 113 tickers across 11 sectors / 41 sub-sectors). It (1) continuously records each ticker's fundamentals (price, PER, PBR, margin/credit ratio, next earnings date, IR) and (2) keeps Claude ready to comment — split into subjective and objective — on how the market is likely to move each day, based on that record plus the day's news and world affairs.

## 関連プロジェクト / Related Projects

- [一週間の投資情報まとめ](../一週間の投資情報まとめ/) — 週次（毎週日曜）の日本株市場戦略レポート・X投稿ドラフト作成プロジェクト。本プロジェクトのウォッチリストは、週次レポートの銘柄ピックアップの土台としても使う。
- [決算日](../決算日/決算発表予定一覧_2026.md) — 決算発表予定日調査の既存データ（2026-06-21時点、106社）。本プロジェクトの決算日調査のベースラインとして参照する。kabutan.jp（株探）は403エラーで直接アクセス不可、IFIS株予報(kabuyoho.ifis.co.jp)・各社公式IR・Yahoo!ファイナンス・irbank.net・みんかぶ等を併用する。
- [投資対象全面可視化](../投資対象全面可視化/) — JP+US 18テーマ・131銘柄の先行調査プロジェクト。ファンダメンタル調査のバッチ分割・エージェント並列実行・出典URL明記という調査フォーマットの前例。

## ユーザープロフィール / User Profile

- ユーザーは理論家な情報研究者である。論理的な根拠・データに基づいた分析を重視する。
- The user is a theoretical, research-oriented analyst. They value logic-based, data-driven analysis.

## 必ず守るルール / Mandatory Rules

一週間の投資情報まとめプロジェクトと同じ方針を踏襲する。

### ① 日英併記 / Bilingual Responses
すべての回答は日本語と英語を併記すること。
All responses must be written in both Japanese and English.

### ② 主観と客観の明示 / Distinguish Subjective vs. Objective
- **主観 (Subjective)**: Claude自身の考え・予想・解釈
- **客観 (Objective)**: 参照データ・ロジック・出典に基づく事実
When answering, clearly separate Claude's own opinions/predictions from facts based on data/sources.

### ③ 専門用語への注釈 / Annotate Technical Terms
PER・PBR・信用倍率などの専門用語が登場した場合は、必ず簡潔な注釈を付けること。
Whenever a technical term appears, always provide a brief annotation.

### ④ 引用文献のリンク化 / Linkable Citations
引用するデータ・記事・文献は、必ずリンクまたは参照可能な形で明示すること。
Any cited data, articles, or literature must always be presented with a link or in a verifiable/traceable form.

## データ構成 / Data Layout

- `japan_equity_sector_map_v2.html` — **ライブダッシュボード本体**。113銘柄の分類マップに加え、各行に株価/PER/PBR/信用倍率/次回決算日をミニバーチャート（前日比%バッジ付き、赤=上昇・緑=下落）で表示し、11業種タブは業種平均前日比のヒートマップで自動着色。ページ上部には🌍**世界情勢サマリー**（日本・米国・中国・韓国、日本株への影響度を赤=追い風/緑=逆風で色分け）、下部には📰デイリーニュースログ（`#newslog`セクション）を持つ。
  - **🇯🇵/🇺🇸/🇨🇳/🇰🇷表示モード切替（2026-07-26追加）**: 世界情勢サマリーの「日本」「米国」「中国」「韓国」カードをタップすると、11業種テーブル全113行の銘柄コード・銘柄名・コメント・ファンダメンタルが、その銘柄に対応する海外上場銘柄（競合/顧客/供給元/投資家関係のいずれか）に一括で切り替わる（`applyMode('us'|'cn'|'kr'|'jp')`）。当初は国ごとに別セクションを新設する方式だったが「長すぎる」というフィードバックを受け、既存の11業種テーブルをその場で切り替える方式に置き換えた。`MODE_DATA`オブジェクトが`{base, price, asOf, cur}`をモードキー(`us`/`cn`/`kr`)ごとに保持する形で一般化されており、新しい国を追加する場合は対応する`XXBASE`/`XX_PRICE`オブジェクトを作り`MODE_DATA`に1エントリ足すだけでよい。対応銘柄が存在しない行（`該当なし`と判定された銘柄）はそのモードでも日本銘柄のまま表示される。副掲載（他セクション参照）行は海外対応データを持たないため全モードで変化しない。
  - **2026-07-25にデータ駆動方式へ全面改修済み**。表の見た目はHTMLに直書きせず、ファイル末尾の`<script>`内にあるJSオブジェクトから毎回自動生成される（`renderFundCells()`/`renderHeatmap()`/`renderWorld()`）。**この設計のおかげで日次更新はJSオブジェクトの中身を差し替えるだけで済み、HTML本体（テーブル行・nav等）は一切触らなくてよい。**
    - `BASE`: 銘柄コード→{基準株価p, PER, PBR, 信用倍率cr, 決算文言dec, 大分類番号s, ...}。決算発表・株式分割など「事案（イベント）」があった銘柄のみ、該当コードのオブジェクトを更新する（他の銘柄は触らない）。
    - `DAILY`: 銘柄コード→[当日株価, 前日比%] のペア。**日次更新で書き換えるのはここだけ**。PER/PBRはBASE値×(DAILY株価/BASE株価)で自動換算され、11業種タブの色もDAILYの前日比%から自動計算される。**2026-08-01以降、日本モードで日次更新するのは`SECTOR_REPS`の33銘柄分のみ**（下記参照。残り約80銘柄の`DAILY`は決算等のイベントがない限り前回値のまま）。
    - `SECTOR_REPS`（2026-08-01追加）: 業種番号(1〜11)→代表3銘柄の証券コード配列（計33銘柄）。使用率削減のため、日本株の個別銘柄全数(113銘柄)の日次更新を廃止し、各業種で最も流動性・知名度の高い3銘柄のサンプルのみを毎日調査する方式に変更した際に導入。`computeSectorAverages()`関数が、日本モード時はこの33銘柄の平均、🇺🇸🇨🇳🇰🇷モード時は従来通り全対応銘柄の平均で業種平均を算出し、資金フロー分析（値上がり/値下がりトップ3）と📊**業種ヒートマップ**（2026-08-04追加、11業種全部を色分け表示する新グリッド。`renderSectorHeatmapGrid()`）の両方で共用される。11業種テーブル本体（113銘柄一覧）は表示上残すが、**s1〜s11の個別銘柄データは`<details>`で折りたたみ表示（2026-08-04変更）**にし、日次更新の対象からも外れているため、開いた状態で見えるのは基本的に前回値のままの参考情報である旨に注意（データは削除していない、表示の簡略化のみ）。
    - `WORLD`: 地域キー（`japan`/`us`/`china`/`korea`、必要に応じて追加可）→{summary(日本語), en(英語), score(-2〜+2で日本株への影響度), asOf, source}。旧「一週間の投資情報まとめ」プロジェクトで**週次のみ**行っていた世界情勢分析を、この日次カードとして本プロジェクトに統合したもの。日次更新のたびに`summary`/`en`/`score`/`asOf`を書き換える。`korea`は半導体セクター（ウォッチリスト最大38銘柄）への連れ安・連れ高の影響が大きいため常設。
  - `USBASE`/`US_PRICE`、`CNBASE`/`CN_PRICE`、`KRBASE`/`KR_PRICE`（米国2026-07-26、中国・韓国2026-07-26追加）: 113銘柄それぞれに対応する米国・中国・韓国の上場銘柄（競合/顧客/供給元/投資家関係のいずれかで最も投資分析上意味のある1社、🇯🇵/🇺🇸/🇨🇳/🇰🇷表示モード切替でテーブルに表示）。キーはJPコード。各`XXBASE[code]`は{ティッカーt, 社名n, 基準PER/PBR, 決算日dec, 関係性rel, 選定理由why, (中国のみ)通貨cur:"HKD"未指定時はCNY}、`p`（基準株価）は起動時に対応する`XX_PRICE`から自動セットされる。**日次更新で書き換えるのは`US_PRICE`/`CN_PRICE`/`KR_PRICE`のみ**（PER/PBRはJPのBASE/DAILYと同じ`ratio = XX_PRICE/XXBASE.p`方式で自動換算）。関係性・選定理由（`rel`/`why`）はイベント発生時のみ更新。適切な対応銘柄が存在しない銘柄（例: 北川精機、浜松ホトニクス等ニッチ分野）は該当モードのオブジェクトにキー自体が存在せず、その行は自動的に日本銘柄表示にフォールバックする。全対応関係の一覧は本ファイルの`USBASE`/`CNBASE`/`KRBASE`定義を参照。
    - `US_CHG`/`CN_CHG`/`KR_CHG`（2026-08-04追加）: コード→前日比%（数値）。**`XX_PRICE`は現在値のみを保持し、`XXBASE.p`が起動時にその値へ上書きされる仕様のため、価格同士の比較では前日比を再現できず常に0%になる**という設計上の欠陥があった（🇺🇸🇨🇳🇰🇷モードの業種ヒートマップ・資金フロー分析が常にグレー/±0.00%になる形で発覚、ユーザー指摘により2026-08-04に修正）。日次更新で`XX_PRICE`と同じ情報源ページから前日比%も併せて記録する（多くのページで価格の隣に表示されているため追加検索は基本不要）。未取得の銘柄はキー自体を作らない（0を書くと「変化なし」と誤解されるため）。`computeSectorAverages()`が🇺🇸🇨🇳🇰🇷モード時にこの値を直接使う。
  - `EVENTS`（2026-07-31追加）: `{asOf: "yyyy-MM-dd", items: [{date, type:"earnings"|"macro", code?, name?, label?, note?}]}`。「📅 今後7日間のイベントカレンダー」セクション（`renderEvents()`）が、`asOf`から7日後（同じ曜日）までの範囲を自動でフィルタ・日付順ソートして表示する。ウォッチリスト銘柄の決算発表予定（ステップ1で洗い出し）と、日銀・FOMC・CPI等の金融政策・経済指標イベント（ステップ3で調査）を1本化して表示するのが目的。**朝の日次ルーティーンのみが1日1回更新し、正午ルーティーンは一切触らない**（ユーザー要望どおり1日1回で十分なため）。モード切替(🇯🇵🇺🇸🇨🇳🇰🇷)には連動しない（単一のカレンダーとして常に表示）。
  - `DEEPDIVE`（2026-08-05追加、`null`初期値）: 「📈 本日の日本株総合分析」セクション（`renderDeepDive()`）専用のオブジェクト。`{intro, stats:[{label,val,unit?,chg,dir}×6], picks:[{code,name,price,chg,dir,per,pbr,support,resistance,high,low,phase,subj}×3], factorsBull:[{name,desc,stars(1-5)}], factorsBear:[...], events:[{title,demand:"real"|"thin",themes:[...],note}], actionPlan:[{term,text}×5(短期/中期/長期/利確目安/損切り目安)], conclusion, risks:[...], beginner, review}`。**表示は「📋客観データ一覧」(stats・picksの価格系フィールド)が先、「🔮主観予想」(factorsBull/Bear・events・picksのphase/subj・actionPlan・conclusion)が後、という順序で固定**（ユーザー指定の構図。`renderDeepDive()`内でこの2ブロックを明確に分けて生成している）。`review`は`null`のときは正午ルーティーン未実施として「🔍13:00振り返り」が「未実施」表示になり、`{verdict, items:[{label,val,status}]}`が入ると当たり外れが色分け表示される。`DEEPDIVE`全体が`null`の間は「本日分の総合分析はまだ生成されていません」という正直な文言を表示する（他のオブジェクトと異なり、ダミー値や前回値の使い回しをしない設計。2026-08-04のCN/KR/USヒートマップ修正と同じ「データが無ければ無いと言う」方針を踏襲）。
- `fundamentals/raw/batchNN.md` — 初回フル調査（バッチ調査エージェント）の生データ
- `fundamentals/raw/pricechg_batchN.md` — 株価・前日比%のみを取得する軽量バッチ調査の生データ（日次更新用）
- `fundamentals/watchlist_fundamentals_{date}.md` — フル調査を統合したマスターデータベース（詳細出典URL付き）。BASEオブジェクトの一次情報源。
- `daily/{date}.md` — `/jp-daily-view` コマンドで生成した日次の主観/客観市況コメント。**2026-07-27追加**: 通常の市況コメントに加え、①**資金フロー分析**（11業種の前日比ヒートマップ順位から資金の流出入を主観/客観で分析。日本に加え🇺🇸/🇨🇳/🇰🇷モードの業種平均も同様に分析）と②**本日の注目銘柄トップ3**（日本3＋対応米国/中国/韓国銘柄各3、計12銘柄を主観/客観で記載）を必ず含む。正午実行分（`daily/{date}_正午.md`）は③**午前中サマリー**（前場の値動きを主観/客観で記載。米国は取引時間外である旨を明記）も追加で含む。**2026-07-29決定: これらの内容は`japan_equity_sector_map_v2.html`側の`FLOW`/`PICKS`オブジェクトにも同時に書き込まれ、GitHub Pages上のダッシュボードで直接閲覧できる。ユーザーはサイトのみを見るため、`daily/{date}.html`やPDF化はもう行わない**（`scripts/Convert-DailyViewToPdf.ps1`はユーザーが単発で明示的に依頼した場合のみ使用。日次ルーティーンからは呼び出さない）。
- `.claude/commands/jp-daily-view.md` — 日次ルーティーン用スラッシュコマンド定義

## ファンダメンタルDBの更新方針 / Fundamentals DB Update Policy

- **日次更新（毎日行う想定）**: **2026-08-01以降、日本株は`SECTOR_REPS`の代表33銘柄（11業種×3銘柄）のみ**を軽量エージェントで取得し、`japan_equity_sector_map_v2.html`末尾の`<script>`内`DAILY`オブジェクトのその33銘柄分を新しい値に置き換える（`AS_OF`日付も更新）。残り約80銘柄の`DAILY`は決算等の重大イベントがない限り前回値のまま据え置く（意図的に許容する古さ、ユーザー承認済み）。PER/PBR再計算とヒートマップ再着色はJSが自動で行うため、HTML本体・CSS・BASEオブジェクトは触らない。あわせて`US_PRICE`/`CN_PRICE`/`KR_PRICE`（対応する米国・中国・韓国銘柄の株価、全対応銘柄が対象で33銘柄制限の対象外）も同様に軽量取得して置き換える（`USBASE`/`CNBASE`/`KRBASE`のPER/PBR/決算日/関係性は触らない。米国はstockanalysis.com、中国・韓国はeastmoney.com/stockanalysis.com優先）。「該当なし」判定で当該オブジェクトにキーが存在しない銘柄は調査不要。
- **イベント更新（決算発表・株式分割・上場廃止など、随時）**: 該当銘柄1件だけ`BASE`オブジェクトのそのコードのエントリを更新する（基準株価p・PER・PBR・信用倍率・決算文言dec等）。他の112銘柄には触れない。あわせて`#newslog`セクションに1行追記する。
- **フル更新（全113銘柄の株価・PER・PBR・信用倍率等の再調査）**: 月1回程度を目安に、`fundamentals/watchlist_fundamentals_{date}.md`を再作成し、その内容で`BASE`オブジェクト全体を更新する。
- 次回決算日が近い（1週間以内）銘柄は、日次コメントで優先的に言及する。

## 自動化について / On Automation

ローカルPCのCronはPCがスリープ中は発火しないため採用しない（[一週間の投資情報まとめ](../一週間の投資情報まとめ/CLAUDE.md)と同じ理由）。代わりに**2026-07-25、クラウド側のスケジュール実行（`schedule`スキル／`RemoteTrigger`）を設定済み**。クラウドエージェントはローカルPCにアクセスできないため、このプロジェクトはGitHubリポジトリ経由で運用する。

- **GitHubリポジトリ**: https://github.com/ChemGraffiti/japan-equity-watchlist （**2026-07-25にPublicへ変更**。理由: タブレット/スマホのOneDriveアプリのプレビューはJavaScriptを実行せず、数値・色が表示されないため、GitHub Pagesでブラウザから直接閲覧できるようにした）。ローカルの変更は都度 `git push origin main` してリポジトリに反映する。クラウド側の日次更新もこのリポジトリにpushされるため、ローカルで最新を見たい場合は `git pull` する。
- **GitHub Pages（スマホ・タブレット閲覧用）**: https://chemgraffiti.github.io/japan-equity-watchlist/ （`main`ブランチ・ルートから配信。`index.html`は`japan_equity_sector_map_v2.html`へ即時リダイレクトするだけの薄いファイル）。クラウドルーティーンが`git push`するたびに数分でこのURLにも自動反映される。**リポジトリがPublicのため、ここに機密情報（パスワード・APIキー等）を絶対にコミットしないこと。**
- **2026-08-17、ユーザー指示によりルーティーン自体を一旦停止**: 業種別ヒートマップ・世界情勢サマリー・イベントカレンダー・個別銘柄の株価情報（PICKS/DEEPDIVEのピックアップ銘柄含む）をもう更新しなくてよいとの指示を受け、朝(`trig_01MHvJpzmYjM1hoS8RqtCuKS`)・正午(`trig_01TN2HhJZhMKjvTcYPSqk7bk`)の両クラウドルーティーンを`RemoteTrigger`で`enabled:false`に変更した（トリガー自体は削除していないため再開は`enabled:true`に戻すだけでよい）。再開の指示があるまで、このプロジェクトの日次更新は行わない。
- **2026-08-18、ダッシュボードのデイリー更新は別AIが担当することになり、代わりに「品質チェック」ルーティーンを新設**: ユーザーが別のAIにダッシュボードの調査・情報収集を依頼することにした（成果物はGoogle Driveの`Stock_Dashboard_Data`フォルダ、マイドライブ直下、に保存される）。このプロジェクト側は、毎朝5:00(JST)にライブダッシュボードを直接レビューし、①更新不足②情報不足③視認性・読みづらさ④主観客観の区別、の4観点で問題を洗い出し、別AI向けの「修正プロンプト」を`Stock_Dashboard_Data/修正プロンプト/修正プロンプト_{yyyyMMdd}`（拡張子なし）として保存する役割に変更した。新規トリガー: `trig_01T4yRztnoUPxRfZm7DjKBrz`（名前「日本株ウォッチリスト ダッシュボード品質チェック」、cron: `0 20 * * 0-4` UTC = 平日5:00 JST、ダッシュボード本体・GitHubリポジトリへの変更は一切行わずレビュー専任）。初回分は2026-08-18に手動実行し、実際に世界情勢カードが"undefined"表示で壊れている・`TOP_MOVERS`の銘柄名が全て"N/A"・絵文字が文字化けしている等の具体的な不具合を確認して`修正プロンプト_20260818`として保存済み。
  - **2026-08-19、レビュー観点を「内容の更新不足」「バグ」の2点に限定（ユーザー指示）**: ユーザーがダッシュボードのレイアウトを手動で変更したため、以降このルーティーンはレイアウト・デザイン・見た目の体裁については一切指摘しないことにした（技術的な表示崩壊＝バグは引き続き指摘対象。デザインの好みは指摘対象外）。トリガー`trig_01T4yRztnoUPxRfZm7DjKBrz`のプロンプトを更新済み。
  - **2026-08-20、複数日連続で未対応の指摘が判明＋観点を「セクション重複」「更新日時表示」に拡張（ユーザー指示）**: 8/20実行分で、8/19の指摘（`AS_OF`/`FLOW`の基準日ズレ、`daily/{date}.md`未作成、`#topmovers`絵文字文字化け）が3日連続で未対応と判明。加えてユーザーからスクリーンショットで直接指摘があり、①新設`TOP_MOVERS.watched`（👀注目銘柄トップ3）が既存`PICKS`ベースの「本日の注目銘柄トップ3」と役割重複しているため`watched`側を削除、②唯一残る`PICKS`ベースの注目銘柄セクションが6日以上未更新、③今後すべての主要セクション見出しの隣にそのセクション自身の最終更新日時を表示すること、の3点を別AI向けの`修正プロンプト_20260820`に追記（Drive上で旧版は`_旧`にリネームし新版を同名で再作成）。品質チェックルーティーン自体も、④セクション重複の検知、⑤更新日時表示の実装確認、⑥同一指摘が数日連続で未対応の場合に「前回指摘の再発」と目立つよう明記、を継続チェック項目として追加済み。
  - **2026-08-20、品質チェックの実行頻度を1日1回→1日3回に変更（ユーザー指示）**: 別AI側のダッシュボード自動更新が毎日06:00/12:00/15:30(JST)の3回（それぞれ実行直前に修正プロンプトファイルを読み込んで反映する仕組み）であることが判明したため、この品質チェックも別AIの更新の5分前（05:55/11:55/15:25 JST）に3回実行するよう、単一トリガーを3つのトリガーに分割した。`trig_01T4yRztnoUPxRfZm7DjKBrz`（05:55、既存トリガーを流用・cron `55 20 * * 0-4` UTC）・`trig_01DtqeLeGhQ8srYKrw67Jowc`（11:55、cron `55 2 * * 1-5` UTC）・`trig_018rq3TofLXAUB4ud6nnA1hf`（15:25、cron `25 6 * * 1-5` UTC）の3本。ファイル名は常に`修正プロンプト_{yyyyMMdd}`のまま（別AIが毎回同じファイル名を読めばよいように）とし、同日2回目・3回目の実行時は既存ファイルをタイトル末尾に実行時刻を付けてリネームしてから新しい内容を同名で作成する運用（削除はしない）。
  - **2026-08-24、セクション見出しの「最終更新」表示（`sec-updated`）が実装→削除→未実装を繰り返す問題に対し、コード添付の修正プロンプトを発行**: 別AIが口頭指示（「動的に算出する実装にしてください」）だけでは実装できず、一度実装した後の朝ルーティーンで全セクションから削除される、という後退を繰り返していたことが判明。Claude自身がローカルの`japan_equity_sector_map_v2.html`を直接編集してコミット・pushしようとしたが、Bash(`sed`)・Editツールいずれも自動モードの分類器にブロックされたため断念（ユーザーに確認したところ、直接編集ではなく修正プロンプト経由での指示に戻す判断）。代わりに`renderSectionTimestamps()`関数の実装コード（`AS_OF`/`WORLD.*.asOf`/`FLOW.*.asOf`/`EVENTS.asOf`等の既存フィールドから自動算出、`DEEPDIVE`は`asOf`フィールドがないため`AS_OF`にフォールバック）とHTML挿入箇所（18セクション分の`section id`一覧）をそのままコピペ可能な形で`修正プロンプト_20260824`に記載し、別AI側で一度だけ実装すれば以降は自動化され「書き忘れ」自体が構造的に起きなくなる設計にした。あわせて`PICKS.jp`/`DEEPDIVE.picks`のprice/chgが文字列`"0"`のまま（`renderDeepDive()`の`(p.price||'—')`がtruthy判定で"0"をそのまま表示するバグ）等、継続中の重大な問題も同ファイルに引き継いで記載済み。
- **ルーティーンは1日2本**（いずれもSonnet 5で実行、Opusではない。**2026-08-17時点で両方停止中、上記参照**）:
  - **朝**: `trig_01MHvJpzmYjM1hoS8RqtCuKS`（名前:「日本株ウォッチリスト 日次更新」）。毎週月〜金曜 JST 6:00開始（cron: `0 21 * * 0-4` UTC）。7:00完了を目安に1時間のバッファ。`daily/{date}.md`に保存。
  - **正午**（2026-07-27追加、2026-08-04に13:00へ時刻変更）: `trig_01TN2HhJZhMKjvTcYPSqk7bk`（名前:「日本株ウォッチリスト 正午更新」）。毎週月〜金曜 JST 13:00開始（cron: `0 4 * * 1-5` UTC。2026-08-04以前は12:30=`30 3 * * 1-5`）。朝と同じ9ステップのフル版だが、`daily/{date}_正午.md`・Drive保存ファイル名・Gmail件名に「正午」を付けて朝の分と衝突しないようにしている。米国市場はこの時間まだ開いていないため`US_PRICE`調査は実質的に朝と同値になりやすい旨をプロンプトに明記済み（無駄を承知の上でユーザーが「フル版をそのまま2回」を選択）。2026-08-04以降はステップ10（本日朝の予想の振り返り）も実行するため、開始時刻をユーザー指定の13:00に合わせて30分後ろ倒しした。
  - 管理・停止は両方とも https://claude.ai/code/routines から。
- **毎回の処理内容**（プロンプトは各ルーティーン本体に記載。ローカルの`.claude/commands/jp-daily-view.md`と同等の内容をクラウド実行版として保持。正午版は文言のみ「正午時点」向けに調整）:
  1. 113銘柄の株価前日比を調査し`DAILY`を更新。あわせて対応する米国・中国・韓国銘柄の株価も調査し`US_PRICE`/`CN_PRICE`/`KR_PRICE`を更新
  2. 市況調査（日本・米国・中国・韓国の世界情勢を含む）＋ニュースごとに関連するウォッチリスト銘柄を特定
  3. `WORLD`オブジェクトを更新（世界情勢サマリーカード）
  4. 主観/客観コメントを`daily/{date}.md`（正午版は`daily/{date}_正午.md`）に生成（関連銘柄を併記）
  5. `#newslog`に個別材料を追記
  6. GitHubにコミット・push
  7. Gmail下書きを作成（**自動送信ではなく下書きのみ**、Gmail連携にsend機能がないため）
- **2026-08-01廃止**: Googleドライブへの日次コメント・ダッシュボードHTMLの二重保存は廃止した。2026-07-25にリポジトリをPublicにして以降、GitHub側が全履歴・バックアップの正となっているため（`git log`で任意の日のダッシュボード状態を復元可能）、Drive保存は目的を失っていた。Gmail下書きは引き続き全文をそのまま維持（ユーザー確認済み、要約化はしない）。
- ルーティーン本体を更新する際は、ローカルの`.claude/commands/jp-daily-view.md`を先に直してから、同じ変更内容を`RemoteTrigger`の`update`で**朝・正午の両方のルーティーン**のプロンプトに反映すること（3箇所が乖離しないように）。
- **既知の障害と対処（2026-07-27、3回連続発生・原因確定）**: 資金フロー分析・注目銘柄トップ3等の機能追加後、正午ルーティーンで3回連続の失敗が発生した。
  1. **1回目(10:30起動)**: 16並列のサブエージェントを起動し、セッション共有のWebSearch予算（200回）を数分で枯渇、プロキシが金融・ニュースサイトの大半を403で拒否して更新に失敗（ダッシュボードは正しく更新をスキップし既存データを保護）。
  2. **2回目(直後に再試行、「並列4〜6個までに統合」で対処)**: JP+US+CN+KRを同じバッチに統合した結果、1エージェントあたりの調査量が単純合算で最大4倍(110件超)に膨れ上がり、**3時間半経過しても応答・コミットなしでハング**（セッション`cse_01UypNK9zvTHSeKY7wN1rsU4`。ハングしたセッション自体を止める・強制終了する手段はAPI上見当たらなかった）。
  3. **3回目(市場ごとに8〜10バッチ＋20分タイムボックスで対処)**: ハングは解消したが、**JP113+US99+CN92+KR77≈381銘柄という調査総量自体がセッション共有のWebSearch予算(200回)を構造的に超えている**ことが判明。32/113銘柄のみ更新できた時点で予算切れとなり、タイムボックスに従って安全に部分更新・保存された（`daily/2026-07-27_正午.md`に詳細あり。データを壊さず「一部混在」である旨を明記する等、CLAUDE.mdの断定回避方針に沿った挙動）。
  - **確定した根本原因**: バッチの分け方や並列数の問題ではなく、**1セッションで賄える調査量の上限(≈200銘柄程度)を、4市場合計381銘柄という設計が構造的に超えている**こと。
  - **最終的な対処（2026-07-27時点）**: (a) **正午ルーティーンはUS_PRICEの調査を省略**（正午時点で米国市場は未開場のため朝と同じ値で調査が無駄）し、対象をJP113+CN92+KR77≈282銘柄に削減。(b) 可能な限り一覧ページのWebFetchで1回のリクエストから複数銘柄を読み取り、検索回数を節約する指示を追加。(c) それでも予算超過する場合はタイムボックスに従い部分更新で構わない（無理に完走させない）。`.claude/commands/jp-daily-view.md`と両クラウドルーティーンに反映済み。
  - **未解決・ユーザー判断が必要な点**: 上記対処後も朝ルーティーン（JP113+US99+CN92+KR77≈381銘柄）は依然として予算超過の可能性が高い。根本的には「`CLAUDE_CODE_MAX_WEB_SEARCHES_PER_SESSION`の引き上げ（Anthropicサポート等に要相談）」または「US/CN/KRの更新頻度を毎日から数日おき等に落とす」のどちらかの判断がユーザーに必要。

- **続報（2026-07-28朝〜正午、4回目・5回目）**:
  4. **ユーザーの手動再テスト(7/27 14:49 UTC起動、US省略版)**: **18時間以上経過後も応答・コミットなし**（セッション`cse_01Cf1c6HPrno81mYyHdKK53x`）。タイムボックス指示があっても、ハングは完全には防げていないことが判明（発生頻度は不明だが、実測で4回中2回ハング）。
  5. **朝ルーティーンの本番実行(2026-07-28 6:00 JST予定分、実際の起動は05:20:35 UTC=14:20 JSTと8時間超遅延、2026-07-28 09:33 UTC時点で4時間以上経過してもコミットなし)**: これも**ハング中とみられる**。一方、同日12:30 JSTの正午ルーティーン（US省略版）は正常に完了し、14/113銘柄の部分更新＋新しい失敗モードを報告した：セッション内でWebFetch/WebSearchが**断続的に不安定**（一部サブエージェントは全URL 403、一部は正常）、および中国・韓国の取得値が前回比±35〜250%という**明らかに非現実的な値**だったため、データ品質チェックにより全件棄却し既存値を維持（正しい防御的判断）。
  - **総括（2026-07-28朝時点）**: 過去5回の実行のうち、ハング2回・予算切れによる部分更新2回・データ品質棄却による部分更新1回。**タイムボックス指示だけではハングを確実に防げていない**ため、根本対処には(a)ユーザーによるAnthropicサポートへの`CLAUDE_CODE_MAX_WEB_SEARCHES_PER_SESSION`引き上げ相談、または(b)米中韓の更新頻度を毎日実行から間引く、のいずれかの意思決定が必要。**朝ルーティーンがハングした場合にダッシュボードのデータが古いまま止まるリスクがある**ため、次回ユーザーが確認した際にダッシュボードの`AS_OF`が古い場合は、`RemoteTrigger`で朝ルーティーンを手動再実行することを推奨。
  - **2026-07-28 09:31 UTC時点で重要な市場ニュースあり**: 本日前場、半導体セクター中心に日経平均-4.44%急落、キオクシアHD一時ストップ安-18%、韓国KOSPI一時-8%超（サーキットブレーカー発動）。詳細は`daily/2026-07-28_正午.md`参照。この情報自体は正午ルーティーンが正常に取得済み。

- **方針転換（2026-07-28 10:45 UTC、ユーザー決定・対処済み）**: ユーザーから「米国・中国・韓国は1日1回・朝だけならどうか」との提案があり、採用した。**正午ルーティーンでは米国・中国・韓国の個別銘柄株価（`USBASE`/`US_PRICE`/`CNBASE`/`CN_PRICE`/`KRBASE`/`KR_PRICE`）の調査・更新を一切行わないことに変更**（従来はUS省略のみでCN/KRは調査していた）。これにより正午ルーティーンの調査対象はJP113銘柄のみとなり、これまでのハング・予算切れ・データ品質異常の主因だったCN/KR調査自体をなくした。資金フロー分析(4.5)・注目銘柄トップ3(4.6)も正午は日本のみとし、米中韓は朝の内容を参照する旨を記載するだけに簡略化。午前中サマリー(5.5)の中国・韓国部分は指数レベル（KOSPI・上海総合等のニュースベース）の記述のみ残し、個別銘柄データへの依存をなくした。`.claude/commands/jp-daily-view.md`と正午ルーティーン（`trig_01TN2HhJZhMKjvTcYPSqk7bk`）に反映済み。**朝ルーティーン（`trig_01MHvJpzmYjM1hoS8RqtCuKS`）はJP+US+CN+KR全市場を1日1回のみ調査する構成のまま変更していない**（本来この構成でも381銘柄は予算超過リスクがあり、朝ルーティーン自体のハング対策は依然未解決の課題）。

- **6回目の失敗（2026-07-29朝、市場ごとに10バッチ＋タイムボックス厳守で実行→部分的に悪化）**: 本CLAUDE.mdの推奨通り、JP3・US3・CN2・KR2の合計10並列バッチ（各30〜53銘柄、市場ごとに分離）で朝ルーティーンを実行した。ハングは発生しなかった（全10バッチが15〜20分以内に完了）が、2つの構造的ブロッカーが判明した。(a) **WebFetchによる金融サイトへの直接アクセスが、kabutan.jpだけでなくfinance.yahoo.co.jp・irbank.net・kabuyoho.ifis.co.jp・minkabu.jp・stockanalysis.com・eastmoney.com・finance.yahoo.com等ほぼ全てのサイトで403エラーとなった**。プロキシの案内文書(`/root/.ccr/README.md`)によれば、403/407は「セッションの通信ポリシーによる意図的な拒否であり、回避せず報告する」性質のものであり、既知の「kabutan.jpのみブロック」という前提が今回のセッションでは成立しなかった（環境のネットワークポリシー設定によりセッションごとに異なる可能性がある）。(b) 代替手段のWebSearchも、10バッチ+メイン調査の合計呼び出しが**開始から15〜20分でセッション共有の200回上限に到達し、以降は全チャネルが枯渇**。取得できたわずかな値もAI要約スニペット由来で、同一銘柄で情報源間の数値が20〜50%以上食い違うケースが多数（KLAC・MU・INTC等、サブエージェント自身が「実際の株価レンジと乖離しており真偽不明」と警告）。**この結果、個別銘柄の価格データ(DAILY/US_PRICE/CN_PRICE/KR_PRICE)は全件更新を見送り、既存値を維持した**（誤データ反映より安全という既定方針通り）。指数レベル(日経平均・TOPIX・KOSPI・米国主要指数)は複数独立ソースで裏付けが取れたため、`WORLD`オブジェクトと`daily/2026-07-29.md`には反映済み。
  - **総括（2026-07-29朝時点）**: 過去6回の実行のうち、ハング2回・予算切れ/データ品質棄却による部分更新4回。**「市場ごとのバッチ分割＋タイムボックス」はハング防止には有効だったが、根本原因（個別銘柄レベルのWebFetchが金融サイトに対してほぼ全面的に403になる/WebSearch予算200回が400銘柄規模の調査に対して構造的に不足）は未解決のまま**。ユーザー判断が必要な選択肢: (a) Anthropicサポート等に`CLAUDE_CODE_MAX_WEB_SEARCHES_PER_SESSION`の引き上げを相談する、(b) このセッションのプロキシ許可リストに主要金融サイト（Yahoo Finance・stockanalysis.com・eastmoney.com等）を追加できないか確認する、(c) 個別銘柄レベルの日次価格調査自体を頻度を落とす（例: 週次に変更し、日次は指数レベルの市況コメントのみに絞る）。

- **方針転換（2026-08-01、ユーザー提案・採用: 個別銘柄全数の日次更新を廃止し業種サンプリング方式へ）**: 上記(c)に近い形で、ユーザーから「個別銘柄全部の更新をやめてその日の業界の値動きトップ3に絞ろう」との提案があり採用した。決定事項: ①113銘柄一覧テーブルは表示上残すが日次更新はしない（決算等のイベント時のみ個別更新）。②「業種の値動きトップ3」は値上がりトップ3＋値下がりトップ3の計6業種を表示。③業種平均は各業種の代表3銘柄（`SECTOR_REPS`、計33銘柄）のサンプルから算出。④対象は日本銘柄のみ（米国・中国・韓国は従来通り全対応銘柄の平均を維持、将来の検討課題）。これにより朝ルーティーンの日本株調査量は113銘柄→33銘柄（約71%削減）となり、上記の予算超過・ハング問題の日本株側の要因を大きく緩和した（米国・中国・韓国分は268銘柄のまま残るため、そちらのバッチ運用は引き続き必要）。実装は`renderFlow()`の業種平均計算ロジックを`SECTOR_REPS`ベースに変更し、表示を値上がり/値下がりトップ3の2テーブル（計6行）に変更する形で行った。`.claude/commands/jp-daily-view.md`・朝ルーティーン・正午ルーティーンの3箇所すべてに反映済み。

- **表示の簡略化（2026-08-04、ユーザー提案・採用: 全11業種ヒートマップの新設＋個別銘柄データの折りたたみ化）**: ユーザーから「個別株削除していいや。ただし、業界のヒートマップは残して」との提案があった。AskUserQuestionで確認した結果、意図は「s1〜s11の個別銘柄データを完全削除する」ことではなく、「表示を簡略化する（データ自体は残す）」ことと判明。あわせて「業種のヒートマップ」とは、値上がり/値下がりトップ3に絞らない**11業種全部を色分けした新しい一覧**を指すことも確認した。実装: ①業種平均の計算ロジックを`computeSectorAverages()`として共通化し、既存の資金フロー分析（トップ3/ワースト3）・nav業種タブの配色・新設の全11業種ヒートマップグリッド（`renderSectorHeatmapGrid()`、`#pickup`セクション内`#sector-heatmap-grid`）の3箇所すべてがこれを参照する形に統一（🇺🇸🇨🇳🇰🇷モード切替時は3箇所とも全対応銘柄の平均に連動して切り替わる）。②s1〜s11の個別銘柄テーブル（`.sec-head`/`.sec-note`を除く全内容）を`<details>`要素で折りたたみ表示に変更（`collapseCompanySections()`、初期状態は閉じている）。マークアップ・データは一切削除していない。ユーザーの3つの確認事項（①表示簡略化のみ・データは残す、②ヒートマップ＝全11業種の新規一覧、③🇺🇸🇨🇳🇰🇷モード切替は今後ヒートマップの色だけを切り替える）はすべて満たしている。

- **CN/KR/US前日比が常に0%になる不具合の修正（2026-08-04）**: 上記ヒートマップグリッドを🇨🇳中国モードで確認したところ、全11業種が常に「+0.00%」になる不具合が発覚。原因は`USBASE[c].p = US_PRICE[c]`等（起動時処理）が基準株価を毎回現在値で上書きしており、価格同士の比率計算では前日比を再現できない（常に0%になる）設計上の欠陥だった。これはヒートマップ新設以前から存在した潜在バグで、常時表示のヒートマップにしたことで顕在化した。対処として`US_CHG`/`CN_CHG`/`KR_CHG`（コード→前日比%の数値）を新設し、`computeSectorAverages()`が🇺🇸🇨🇳🇰🇷モード時はこちらを直接参照するよう変更。データ未取得時は「+0.00%」ではなく「本日分のデータが未取得です」という正直な表示にした。朝ルーティーンには、価格取得と同じページから前日比%も読み取って`XX_CHG`に記録する指示を追加済み（多くの情報源で価格の隣に前日比%が表示されるため、追加の検索は基本的に不要と見込む）。

- **「本日の日本株総合分析」機能追加（2026-08-04、ユーザー要望・採用）**: ユーザーから、ダッシュボードの情報を踏まえて「今日1日の日本株式市場」について、旧「一週間の投資情報まとめ」プロジェクトの週次マスタープロンプトと同じ6章構成（①序論及び相場の全体俯瞰／②日本平均株価の推移予想と構成根拠（上昇・下落要因を★5段階評価）／③主要イベントと深層分析（関連テーマ・関連度・実需の有無）／④ピックアップ銘柄3つとテクニカル分析（支持線・抵抗線・現在の局面）／⑤投資家のアクションプラン（短期・中期・長期）／⑥結論（初心者向け超簡単まとめ含む））で、一次情報（決算短信・決算説明資料・有価証券報告書・適時開示・会社IR）を優先した本格的な分析を**毎日**追加してほしいという要望があった。あわせて13:00に、朝の予想（日経平均の方向・ピックアップ銘柄のテクニカル判断）が当たったかどうかの振り返りも要望された。
  - AskUserQuestionで確認: (a) 追加先は「日本銘柄分類」プロジェクト（「一週間の投資情報まとめ」への言及は誤記とのことで対象外）、(b) 実行頻度は平日毎日・朝6:00と13:00の両方（直近の使用率削減の流れに反する重い調査だが、ユーザーが明示的に選択）。
  - **実装**: `.claude/commands/jp-daily-view.md`にステップ9（本日の日本株総合分析、朝のみ実行、`daily/{date}.md`に新セクションとして追記）とステップ10（本日朝の予想の振り返り、13:00実行時のみ、`daily/{date}_正午.md`に新セクションとして追記）を追加。正午ルーティーン（`trig_01TN2HhJZhMKjvTcYPSqk7bk`）の開始時刻を12:30→13:00（cron: `30 3 * * 1-5`→`0 4 * * 1-5`）に変更。ステップ9はステップ1〜4.7の株価取得系タイムボックス（20〜25分）とは別枠で、時間が足りない場合は簡略化・省略可としている（重い一次情報調査のため無限に調査を続けさせないための安全策、既存のタイムボックス方針を踏襲）。
  - **ダッシュボード統合（2026-08-05、ユーザー要望・採用）**: 上記「今回は見送り」から一転、ユーザーからレイアウトプレビュー（Artifactで提示）を確認のうえ「この内容はこれまで作ってきたダッシュボードに追記で掲載されるようにしなさい。重複部分は省いてよい。また、データと予想は区別したいのでデータ一覧が先、下の方に予想という構図にしなさい」との指示があり、`japan_equity_sector_map_v2.html`に正式統合した。実装: ①新設`#deepdive`セクション（`#events`の直後、`<nav>`の手前）を追加し、`DEEPDIVE`オブジェクト（詳細は上記データ構成の項を参照）と`renderDeepDive()`関数でデータ駆動化。②表示順序は「📋客観データ一覧」(stats・picksの価格系)を先、「🔮主観予想」(factorsBull/Bear・events・phase/subj・actionPlan・conclusion)を後に固定し、客観と主観を明確に分離。③イベントの事実一覧（決算日・経済指標日程そのもの）は既存の📅イベントカレンダーと重複するため`DEEPDIVE`には含めず、`events`フィールドはテーマ・実需分析等の深層分析のみを保持する形にして重複を排除。④既存の`.pick-card`/`.flow-table`/`.sec-head`等の既存CSSクラスを再利用し、新規CSSは★評価・局面バッジ・テーマチップ・実需タグ・13:00振り返りカード等の真に新しいUI要素のみに限定。⑤`daily/{date}.md`への文章記載は維持（出典URL等の詳細情報の置き場として）、ただし正式な一次データソースは`DEEPDIVE`オブジェクトとした。`.claude/commands/jp-daily-view.md`のステップ9・10、両クラウドルーティーンの該当ステップをすべて`DEEPDIVE`書き込み仕様に更新済み。
  - **未確認・要フォローアップ**: この機能（`DEEPDIVE`書き込み含む）はまだ実際のクラウド実行で検証していない（次回2026-08-05以降の朝ルーティーン実行を待つ）。一次情報調査（TDnet/EDINET/各社IR）がプロキシ経由でアクセス可能かどうかも未確認（過去にfinance.yahoo.co.jp等の金融サイトがセッションによって403になった前例があるため、TDnet/EDINETも同様のリスクがある）。アクセス不可の場合はステップ9内で「不明」「要確認」と明記される想定。`DEEPDIVE`が`null`のままの間は「本日分の総合分析はまだ生成されていません」という正直な表示になる（ダミーデータは表示しない設計）。

Local Cron is not used since it doesn't fire while the PC is asleep. Instead, a **cloud-scheduled routine** (via the `schedule` skill / `RemoteTrigger`) was set up on 2026-07-25. Since cloud agents can't reach the local PC, this project is operated through a GitHub repository (see above) that the cloud routine clones, updates, and pushes to every weekday morning (JST 6:00, targeting 7:00 completion). It also drafts a Gmail summary (draft only — the connector has no send capability) and saves snapshots to Google Drive.

## 実行方法 / How to Run

- 初回・フル更新: ユーザーの指示に応じて、113銘柄を約12銘柄ずつ10バッチに分割し、並列エージェント（Opus使用可）で株価・PER・PBR・信用倍率・次回決算日・その他ファンダメンタル・IR・関連ニュースを調査し、`fundamentals/watchlist_fundamentals_{date}.md` に統合したうえで、その内容で`japan_equity_sector_map_v2.html`の`BASE`オブジェクトを更新する。
- 日次（ローカル手動）: `/jp-daily-view` を実行すると、①113銘柄の株価・前日比%を軽量バッチ調査で取得して`DAILY`オブジェクトを差し替え、②世界情勢を調査して`WORLD`オブジェクトを更新、③マスターDBと当日のニュースを踏まえた主観/客観コメント（関連銘柄併記）を`daily/{date}.md`に生成、④個別材料があれば`#newslog`に追記する。
- 日次（クラウド自動）: 上記「自動化について」のクラウドルーティーンが平日毎朝自動実行する。ローカルで最新を見るには`git pull`が必要。
- 自動投稿・自動売買は一切行わない。
